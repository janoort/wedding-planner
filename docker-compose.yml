# Docker Compose for local development
# For now, we use Supabase Cloud instead of local Postgres
# Keeping this here for future when we add local services (Redis, etc)

version: '3.8'

services:
  # Postgres is managed by Supabase Cloud
  # To use local Postgres later, uncomment below and update .env.local

  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: postgres
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

# volumes:
#   postgres-data:
